package com.mango.cache.service.impl;

import org.springframework.stereotype.Service;

import com.mango.cache.service.CacheService;


@Service
public class CacheServiceImpl implements CacheService{
	/*@Autowired
	private ProductService  productService;
	@Autowired
	private UserService userService;
	
	@CacheAnnotation
	public List<ProductVo> getListAPIFromCache(String key, String expireTime,ProductRequest productRequest) {
		return productService.getListAPIFromCache(key, expireTime, productRequest);
	}
	
	@Override
	@CacheAnnotation
	public User getResponseUserVoFromCache(String key, String expireTime, String userId) {
		return userService.findByKey(userId);
	}*/
	
}
